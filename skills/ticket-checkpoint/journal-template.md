# Work Log — [#{id}: {title}]({ticket-url})

> Newest entry first. Written only by `/ticket-checkpoint`, and read only by `/ticket-resume`.
>
> An entry is one session's account of itself. It cannot be regenerated from the code, the plan, or git history — so never edit or delete an existing entry. Record a correction as a new entry that says what it corrects.

## {YYYY-MM-DD HH:MM} UTC — {Phase {N}, Step {N}.{M} | Planning | Refinement | Review}

**Where:** `{repo}` @ `{branch}` — HEAD `{sha}` ({subject}), base `{base branch}` @ `{sha}`

**Working tree:** {clean | {N} modified, {N} untracked — uncommitted}{, {N} stash(es)}

**Stopped at:** {the step, and the honest state of it — what works, what does not}

**Next:** {the single next action, specific enough to start on without re-reading anything}

### Done

- {what actually changed, in terms of files and behavior — not what was attempted}

### Decisions

- **{the decision}** — {why, including what it was chosen over}. Affects {Step {N}.{M}}.

### Inferences

- **{what the plan left unspecified}** — {what was assumed, and on what basis}. Affects {Step {N}.{M}}.

### Open questions

- {the question, and who or what can answer it}

### Blockers

- {what is blocking, and what would unblock it}

---

## {previous entry}

...
